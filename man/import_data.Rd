% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_dataset.R
\name{import_data}
\alias{import_data}
\title{Import tabular logger files (CSV/CSV2/TXT) and Excel}
\usage{
import_data(
  path,
  sheet = 1,
  delim = c("auto", ",", ";", "\\t"),
  decimal = c("auto", ".", ","),
  skip = 0,
  comment = NULL,
  col_names = TRUE,
  guess_max = 10000
)
}
\arguments{
\item{path}{File path to the input file.}

\item{sheet}{For Excel files: sheet name or index (default 1).}

\item{delim}{Delimiter for text files: "auto" (default), ",", ";", or "\\t".}

\item{decimal}{Decimal mark for text files: "auto" (default), "." or ",".}

\item{skip}{Integer. Number of lines to skip at the top (default 0).}

\item{comment}{Single character used to indicate comment lines (e.g., "#").
Lines starting with this (after optional whitespace) are ignored. Use \code{NULL}
or "" to disable.}

\item{col_names}{Logical. Whether the first non-skipped, non-comment line
contains column names (default TRUE).}

\item{guess_max}{Passed to readr for type guessing (default 10000).}
}
\value{
A tibble.
}
\description{
Reads text-delimited files (CSV, CSV2, TXT) with auto-detected delimiter
(comma/semicolon/tab) and decimal mark (dot/comma), supports skipping leading
lines and ignoring comment-prefixed lines. Also reads Excel (XLS/XLSX).
}
\examples{
# Text file with metadata header and '#' comments:
# MycoTools::import_data("log.txt", skip = 2, comment = "#")

# Excel first sheet:
# MycoTools::import_data("log.xlsx")

}
